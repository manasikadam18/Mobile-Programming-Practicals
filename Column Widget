import 'package:flutter/material.dart'; 
 
void main() 
{ 
  runApp(const MyApp()); 
} 
 
class MyApp extends StatelessWidget 
{ 
  const MyApp({super.key}); 
  @override 
    Widget build(BuildContext context) 
  { 
    return MaterialApp( 
      home: Scaffold( 
        appBar: AppBar( 
          title: const Text("Column Widget App"), 
            backgroundColor: Colors.deepPurple, 
          foregroundColor: Colors.amberAccent, 
          centerTitle: true, 
        ), 
        body: Container( 
          color: Colors.lightBlue, 
          child: Center( 
            child: Center( 
              child: Column( 
                mainAxisAlignment: MainAxisAlignment.center, 
                children: [ 
                  Text("Manasi Kadam"), 
                  Text("Riddhi Mangaonkar"), 
                  Text("Tanmayi Topale"), 
                  Text("Vatsal Kambale"), 
                  Text("Riya Gajane"), 
                ], 
              ), 
            ), 
          ), 
        ), 
      ), 
    ); 
  } 
  
